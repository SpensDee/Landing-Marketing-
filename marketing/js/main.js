$(function(){
    const mixer = mixitup('.portfolio__content', {
            animation: {
                duration: 938,
                nudge: true,
                reverseOut: false,
                effects: "fade translateZ(-15px) rotateX(90deg) rotateY(90deg)"
            }
    });
});
